# =============================================================================
# .ENV.TEMPLATE - Variables de entorno para el proyecto
# Repositorio: voeseboin-sys/vbn
#
# INSTRUCCIONES:
# 1. Copia este archivo a .env: cp .env.template .env
# 2. Completa los valores con tus datos
# 3. NUNCA subas .env a GitHub (ya está en .gitignore)
# =============================================================================

# =============================================================================
# CONFIGURACIÓN DE GITHUB
# =============================================================================

# Tu nombre de usuario de GitHub
GITHUB_USER=voeseboin-sys

# Tu email de GitHub
GITHUB_EMAIL=voeseboin@gmail.com

# Token de acceso personal (generar en https://github.com/settings/tokens)
# Permisos necesarios: repo, workflow
GITHUB_TOKEN=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

# Nombre del repositorio
GITHUB_REPO=vbn

# =============================================================================
# CONFIGURACIÓN DE JAVA (opcional, auto-detectado)
# =============================================================================

# Ruta al JDK de Java (dejar en blanco para auto-detectar)
# JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64

# =============================================================================
# CONFIGURACIÓN DE ANDROID SDK (opcional)
# =============================================================================

# Ruta al Android SDK (dejar en blanco para usar el de Buildozer)
# ANDROID_SDK_ROOT=/home/usuario/android-sdk
# ANDROID_HOME=/home/usuario/android-sdk

# =============================================================================
# CONFIGURACIÓN DE BUILDOZER
# =============================================================================

# Nivel de verbose (0=error, 1=info, 2=debug)
BUILDOZER_VERBOSE=2

# Modo debug (1=debug, 0=release)
BUILDOZER_DEBUG=1

# =============================================================================
# NOTAS DE SEGURIDAD
# =============================================================================
#
# ⚠️  IMPORTANTE:
# - Este archivo (.env.template) SÍ se sube a GitHub
# - El archivo .env (con tus datos reales) NUNCA se sube a GitHub
# - El token de GitHub es sensible, no lo compartas
# - Si crees que tu token fue comprometido, revocalo inmediatamente en:
#   https://github.com/settings/tokens
#
# =============================================================================
